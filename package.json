{
  "name": "taskapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm --prefix laravel-app run dev:full",
    "dev:assets": "npm --prefix laravel-app run dev",
    "serve": "npm --prefix laravel-app run serve",
    "build": "npm --prefix laravel-app run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
